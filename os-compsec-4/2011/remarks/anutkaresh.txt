2012.01.19.02.23.42

Задание 1. Я понял! В вашей группе нет ленивых людей, поэтому вам ничего
	не стоит написать в двух местах try-catch, вместо того, чтобы
	сгруппировать все источники исключений внутри одного :)

	Ну, ладно... Это не самый плохой вариант из тех, что были.

Задание 2. Мне хочется плакать. Нет, ну честно. Зачем Вы так сложно-то?
	FILE - это и так буферизированный ввод/вывод, зачем его второй раз-то
	буферизировать? Неужели вашу группу этому не учили?

	Но так, вроде правильно... Да и поплакал чуть-чуть :) Полезно для
	слёзных каналов.

Задание 3. Вот же, вот же! Вы же видите, простые вещи, они просты и элегантны.
	Но...

	(1) А почему Вы не все необходимые регистры заполняете перед обращением
		к int 0x13? (Ага, я преднамеренно прощаю первым сдающим
		кое-какие недочёты :)

Задание 4. Ну вот. Даже поплакать не над чем :)

Задание 5. Ага! Самая популярная ошибка:

	(1) Не собирается:

		$ sh cmp.bat 
		main.o: In function `main':
		main.c:(.text+0x43): undefined reference to `mult'
		mult.o: In function `_multnum':
		mult.asm:(.text+0x13): undefined reference to `_print'
		collect2: ld returned 1 exit status

		Посмотрите, пожалуйстка комментарий на счёт опции --prefix
		для NASM, например, у evilenka.txt

Задание 6. Работает!

Задание 7. Не забудьте пожалуйста, что задача сформулирована так: для
	загрузки кода нужно использовать загрузчик из задания 3. Не надо
	снова писать загрузочный сектор.

2012.01.17.17.53.20

Задание 1. 

	(1) sort'у тоже может не хватить памяти для работы, и тогда он
		сгенерирует исключение.

	(2) Задание заключалось в том, чтобы прочитать данные из первых,
		указанных в командной строке файлов, а результат записать
		в последний. То есть, если написано:

			task1 F1 F2 F3 ... FN O

		то прочитать данные надо из Fi, а записать их в O.

Задание 4. В принципе, нормально. Однако:

	(1) У Вас странная и неверная арифметика. Потому что,

		x % y == x & (y - 1)

		только для y равного степени двойки. И если y - степень
		двойки, то

		x / y == x >> N, где y = 1 << N

		А у Вас явно не то написано.
