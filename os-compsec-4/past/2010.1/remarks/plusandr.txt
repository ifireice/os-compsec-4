Задача 3. Хм... Поясните, по какому адресу Вы пытаетесь загрузить данные?

--

Задача 8. Хорошо, что исправили jmp. Но надо ещё поработать над прерываниями.

Задача 9. Алгоритм Евклида - это, конечно, плюс. Но почему Вы ограничиваете обрабатываемые значения 16-тью битами?

Задача 7. Такое решение уже было. При чём, самое неприятное в том, что Вы даже не попытались его запустить, чтобы получить результаты. Нет, ну на самом деле, посмотрите, что должен выдавать run.sh и что у вас записано в results.txt.

Так что, придётся тоже немного переписывать это всё. Сделайте так, чтобы рабочие расстояние между ячейками менялось бы случайно от итерации к итерации, это будет вариант работы номер 3, чтобы как-бы сэмулировать поведение приближенного к реальности сценария обращения к памяти.

Задача 3. Речь шла не о том, чтобы поменять метку памяти (адрес-то тот же самый остался), а о том, чтобы загрузить по другому адресу. Допустим, по адресу 0x7c00 + 0x1230

2011.01.26.12.51.57

Задача 3. Мне такое решение много раз показывали. Надо бы его видоизменить. Например, добавьте место под таблицу описания разделов диска и загрузите строчку по какому-нибудь произвольному адресу.

Задача 4. Эх. Сразу же после включения защищённого режима нужно перезагружать CS. Сра-зу-же : ) Реальный процессор не будет знать, как выполнять sti, ведь в CS с насройками всякой защиты и превилегий будет мусор.

Задача 8. То же замечание про sti.
