2011.06.24.13.07.23

Эх, ну вот зачем так называть директории? UNIX, конечно, позволяет это делать,
но это же совсем не удобно, потому что [ и ] - это значимые для некоторых
оболочек символы, и приходится имена набирать так:

	\[dekker\]

Это несколько неудобно.

Задача 2: То же удивление: как так вы сдаёте задачи, когда исходники не
компилируются?

Задача 4.(-1): То же самое удивление: не компилируется. Кроме этого программа
не работает верно. Когда я исправляю ошибки, то:

	$ clang -O4 select.c -o slct && ./slct 0 < select.c

И получаю стопроцентную загрузку процессора:

	$ ps -C slct -o %cpu
	%CPU
	 100

Как-то это неуютно. А если такая штука будет запущена в центре обработки данных,
где процессорное время стоит денег? Аккуратнее надо быть.

Задача 5: Опять та же самая ерунда... Как Вы проверяли работоспособность
программ? Знаете, есть огромное желание просто не дать Вам допуск. Ну, просто,
это нонсенс какой-то. Такое ощущение, что программа списана, а ошибки в неё
внесены, чтобы меня дурить.

Задача 5.(+1): Аналогично.
