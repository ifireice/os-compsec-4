2012.01.22.17.36.31

Задания 5 и 5(+1). Хорошо

2012.01.17.20.43.08

Задание 2. ОК. Даже очень ОК

Задание 5.

	(1) Ошибки же, pthread может и не сработать.

	(2) Смысл этого задания всё же с оптимизацией скомпилировать и
		увидеть, что алгоритм не работает, хотя и написан верно. Хотя,
		если Вы в run явно написали -O0, то вы на это натолкнулись.
		Ладно. Это не минус

Задание 5.(+1). Барьеры надо расставлять после каждой записи в алгоритме
	Деккера. Нужно гарантировать, что информация о изменении флагов
	дойдёт до другого процессора, перед тем, как будет обращение к
	флагам от другого процессора. А пока работает неверно:

	На двухядерном процессоре:

	-O4 -pthread 1.c -o 1 && ./1 
	1.c: In function ‘func’:
	1.c:12:14: warning: cast from pointer to integer of different size [-Wpointer-to-int-cast]
	199999874

2011.12.27.14.17.34

ЗАДАНИЕ 2. Минусы за:

	(1) Обработка ошибок. Может произойти всякое, почему эти всякие
		результаты не обрабатываются?

	(2) Почему в обработчике сигнала используется нереентерабельная
		функция printf? А что если два сигнала подряд прилетит разных?
	
	(3) Почему sa.sa_mask никак не заполняется? В стеке же, обычно,
		размещается всяческий мусор.
	
	(4) Задание заключалось в том, чтобы проследить, как сигналы
		распространяются по группам процессов.
